import favicon from "../assets/icon.png";
import background from "../assets/bg.jpg";
import {
  title,
  shortName,
  author,
  site,
  language,
  color
} from "./metadata.json";

const common = {
  title,
  shortName,
  author,
  site,
  language,
  color,
  favicon,
  background
};

export { common };
